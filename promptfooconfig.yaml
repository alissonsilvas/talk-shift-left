# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: Avaliação das specs dos módulos Account e Invoice

providers: file://promptfoo/providers.yaml

prompts:
  - file://promptfoo/prompts/spec-review.txt

tests: file://promptfoo/tests/*.yaml
