deptrac:
    paths:
        - ./src
    layers:
        -
            name: Invoice
            collectors:
                -
                    type: directory
                    value: src/Invoice/.*
    ruleset:
        Invoice:
            - Invoice
